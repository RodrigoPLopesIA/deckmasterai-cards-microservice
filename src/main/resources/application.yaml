server:
  port: 8081
spring:
    application:
        name: cards-microservice
    
    mongodb:
        uri: mongodb://root:example@localhost:27017/deckmasterai?authSource=admin
    
    management:
        endpoints:
            web:
                exposure:
                    include: "*"
        endpoint:
            health:
                show-details: always